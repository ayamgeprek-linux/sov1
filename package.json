{
  "name": "sov1",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=@stock-opname/web",
    "build:api": "turbo run build --filter=@stock-opname/api"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}